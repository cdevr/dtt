# Proxmox Server Configuration
PROXMOX_URL="https://192.168.1.2:8006"
PROXMOX_NODE="pve"  # Your Proxmox node name (check in Proxmox UI under Datacenter)

# Proxmox API Authentication (Token-based - Recommended)
TOKEN_ID="root@pam!rootToken"
TOKEN_SECRET="your-token-secret-here"

# Note: The script uses --proxmox-insecure flag by default to skip TLS verification
# This is necessary for self-signed certificates

# OR use Password-based authentication (not recommended)
# PROXMOX_PASSWORD="your-password"

# Proxmox Host SSH Credentials (Required for cloud image download/import)
PROXMOX_SSH_USER="root"
PROXMOX_SSH_PASSWORD="your-proxmox-root-password"

# VM Configuration
VM_ID=100
VM_IP="192.168.1.100"  # Optional - will be auto-detected if not provided

# SSH Configuration for VM Access
SSH_PASSWORD="your-vm-ssh-password"

# Binary to run (optional, can be passed as command line argument)
BINARY_PATH="/path/to/your/script.sh"
